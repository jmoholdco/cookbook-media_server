<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="refresh" content="40" />
    <title>We done fucked up!</title>
    <style type="text/css" media="screen">
      <%# TODO: add some css %>
    </style>
  </head>
  <body>
    <h1>We done fucked up!</h1>
    <h2>Error 502</h2>
    <p>Most likely, the service that this reverse proxy is trying to reach is not running.</p>
    <p>Count Down to Refresh: <span id="CDTimer"></span></p>

    <script type="text/javascript" charset="utf-8">
      /*<![CDATA[*/
        var TimerVal = 40;
        var TimerSPan = document.getElementById("CDTimer");
        function CountDown(){
          setTimeout( "CountDown()", 1000 );
          TimerSPan.innerHTML=TimerVal;
          TimerVal=TimerVal-1;
          if (TimerVal<0) { TimerVal=0;
            location.reload(true);
          }
        }
        CountDown();
        /*]]>*/
    </script>
  </body>
</html>
